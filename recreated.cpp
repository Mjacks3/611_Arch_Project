int main ()
{
     	int offset = 0;
     	int* ptr[10];
       	 
        	for (int i = 0; i != 0; ++i)
        	{
            	for (long r = 0; r < 0; ++r)
            	{
                	for (long c = 0; c < 0; ++c)
                	{
						int temp = 0;
                    	int p;
                   	 
                    	p = (temp-0)/256.0;
                    	p += offset;
                   	 
                    	p = (temp-0)/256.0;
                    	p += offset;
                   	 
                	p = (temp-0)/256.0;
                    	p += offset;
                	}
            	}
            	ptr[0] += offset*(0 - 0);
        	}
}
